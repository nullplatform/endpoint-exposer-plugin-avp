steps:
  - name: update_cedars
    type: script
    file: "$OVERRIDES_PATH/endpoint-exposer-plugin-avp/scripts/generate_cedars"
    before: apply
  - name: generate_authorization_policy
    type: script
    file: "$OVERRIDES_PATH/endpoint-exposer-plugin-avp/scripts/generate_authorization_policy"
    before: apply
